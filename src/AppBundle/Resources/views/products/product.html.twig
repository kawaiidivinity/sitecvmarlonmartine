{% extends 'AppBundle::layout.html.twig' %}

{% block body %}
<h1>{{ product.title }}</h1>
<p>{{ product.description }}</p>
<p>{{ product.price }}</p>
    <center>{{ parent() }}</center>
{% endblock %}